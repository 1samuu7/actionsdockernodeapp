Perfecto Samu 👌 gracias por la captura.
Veo dos cosas importantes:

1️⃣ Has pegado la versión comentada con #(1), #(2), etc.
2️⃣ Estás usando actions/setup-node@v1 y node-version: '18.x'

No está mal, pero vamos a dejarlo limpio y actualizado para evitar problemas.

━━━━━━━━━━━━━━━━━━
🔧 Corrige el archivo test.yml
━━━━━━━━━━━━━━━━━━

Borra todo el contenido y deja exactamente esto:

name: Granja Animales Node.js CI
on:
push:
branches:
- main
pull_request:
branches:
- main
jobs:
build:
runs-on: ubuntu-latest
steps:
- name: Checkout repository
uses: actions/checkout@v4
- name: Use Node.js
uses: actions/setup-node@v4
with:
node-version: '18'
- name: Install dependencies
run: yarn install --frozen-lockfile
- name: Run tests
run: yarn test



